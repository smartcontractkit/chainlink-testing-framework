# Modifies docker compose to pull from a local version of chainlink

services:
  node:
    build:
      context: $CHAINLINK_ROOT
      dockerfile: core/chainlink.Dockerfile
  node-2:
    build:
      context: $CHAINLINK_ROOT
      dockerfile: core/chainlink.Dockerfile
  node-3:
    build:
      context: $CHAINLINK_ROOT
      dockerfile: core/chainlink.Dockerfile
  node-4:
    build:
      context: $CHAINLINK_ROOT
      dockerfile: core/chainlink.Dockerfile
  node-5:
    build:
      context: $CHAINLINK_ROOT
      dockerfile: core/chainlink.Dockerfile