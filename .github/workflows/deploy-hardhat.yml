name: deploy-hardhat

on:
  push:
    tags:
      - v*
    branches:
      - master
      - main
  pull_request:

jobs:
  hardhat:
    name: Deploy Hardhat Test Chain
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        uses: smartcontractkit/deploy-hardhat@v0.1-alpha
