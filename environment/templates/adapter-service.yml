metadata:
  name: dummy-adapter
spec:
  ports:
    - name: access
      port: {{ .Values.adapter.apiPort }}
      targetPort: {{ .Values.adapter.apiPort }}
  selector:
    app: dummy-adapter
  type: ClusterIP
