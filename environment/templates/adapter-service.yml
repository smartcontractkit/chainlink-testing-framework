metadata:
  name: dummy-adapter
spec:
  ports:
    - name: access
      port: {{ .Values.chainlinkCluster.adapter.apiPort }}
      targetPort: {{ .Values.chainlinkCluster.adapter.apiPort }}
  selector:
    app: dummy-adapter
  type: ClusterIP
