metadata:
  name: ganache-network
spec:
  ports:
  - name: access
    port: {{ (getNetwork "ethereum_ganache").RPCPort }}
    targetPort: {{ (getNetwork "ethereum_ganache").RPCPort }}
  selector:
    app: ganache-network
  type: ClusterIP
