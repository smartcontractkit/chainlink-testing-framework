metadata:
  name: ganache-network
spec:
  ports:
  - name: access
    port: {{ .Networks.ethereum_ganache.RPCPort }}
    targetPort: {{ .Networks.ethereum_ganache.RPCPort }}
  selector:
    app: ganache-network
  type: ClusterIP
