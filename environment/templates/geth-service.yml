metadata:
  generateName: geth-network-
spec:
  ports:
  - name: access
    port: {{ (getNetwork "ethereum_geth").RPCPort }}
    targetPort: {{ (getNetwork "ethereum_geth").RPCPort }}
  type: ClusterIP
