metadata:
  name: geth-dev-network
spec:
  ports:
  - name: access
    port: {{ .Networks.ethereum_geth.RPCPort }}
    targetPort: {{ .Networks.ethereum_geth.RPCPort }}
  selector:
    app: geth-dev-network
  type: ClusterIP
