objectmeta:
  name: geth-dev-network
spec:
  ports:
  - name: access
    port: {{ .Values.evm.rpcPort }}
    targetport:
      intval: {{ .Values.evm.rpcPort }}
  selector:
    app: geth-dev-network
  type: ClusterIP
