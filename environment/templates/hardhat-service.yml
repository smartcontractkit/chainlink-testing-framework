metadata:
  name: hardhat-network
spec:
  ports:
  - name: access
    port: {{ .Networks.ethereum_hardhat.RPCPort }}
    targetPort: {{ .Networks.ethereum_hardhat.RPCPort }}
  selector:
    app: hardhat-network
  type: ClusterIP
