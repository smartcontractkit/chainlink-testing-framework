metadata:
  name: hardhat-network
spec:
  ports:
  - name: access
    port: {{ .Values.DependencyGroup.evm.rpcPort }}
    targetPort: {{ .Values.DependencyGroup.evm.rpcPort }}
  selector:
    app: hardhat-network
  type: ClusterIP
