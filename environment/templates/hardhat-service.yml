metadata:
  generateName: hardhat-network-
spec:
  ports:
  - name: access
    port: {{ (getNetwork "ethereum_hardhat").RPCPort }}
    targetPort: {{ (getNetwork "ethereum_hardhat").RPCPort }}
  type: ClusterIP
