- Reviewed error messages, made them easier to understand and more actionable. Removed "github.com/pkg/errors" usage. Added `RevertedErr` sentinel for detecting on-chain transaction reverts with `errors.Is`.